Figtree (`Figtree[wght].ttf`) and Newsreader (`Newsreader[opsz,wght].ttf`) from github.com/google/fonts,
the same families as the web app, under the SIL Open Font License 1.1 (see the OFL-*.txt files).
They are variable fonts; pdf-lib embeds their default instance.
